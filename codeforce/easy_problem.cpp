#include<iostream>
#include<map>
#include<vector>
using namespace std;
class hello {};
int main()
{
    int n; cin>> n;
    vector<int> s(n);
    for(int i=0; i<n; i++){
        cin >> s[i];
    }
    map<int,vector<int,int>> mp;
    int i=0;
    while(s[i]!=)
    return 0;
}