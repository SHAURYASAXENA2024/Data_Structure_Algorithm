// approach is wrong //
#include<iostream>
#include<vector>
#include<math.h>
using namespace std;
class hello {};
int power(vector<int>& nums){
    int sum=0;
    vector<int> res;
    for(int i=0; i<nums.size(); i++){
        if(nums[i]==i){

        }
    }
    for(int i=0; i<nums.size(); i++){
        sum=sum-(res[i])+
    }
}
int main()
{
    int n; cin >> n;
    vector<int> v(n);
    for(int i=0; i<n; i++){
        cin >> v[i];
    }
    
    return 0;
}