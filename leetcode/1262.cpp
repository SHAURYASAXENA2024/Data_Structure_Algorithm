#include <iostream>
#include <vector>
using namespace std;
int maxSumDivThree(vector<int> &nums) {
  if (nums.size() == 0) {
    return 0;
  }
  vector<int> dp(nums.size() + 1);
  dp = {0, 0, 0};
}
int main() { return 0; }
